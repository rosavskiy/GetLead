# 🚀 GetLead - Telegram Lead Generation Bot

**GetLead** — это SaaS-сервис в Telegram для мониторинга сообщений в группах и чатах по ключевым словам. Автоматическая лидогенерация для фрилансеров, агентств и компаний.

## 📋 Возможности

- 🔍 **Мониторинг чатов** — отслеживание сообщений в реальном времени
- 🎯 **Ключевые слова** — гибкая настройка поисковых запросов
- 🚫 **Исключающие слова** — фильтрация нерелевантных сообщений
- 🔧 **Логические фильтры** — операторы AND/OR для точного поиска
- 🤖 **AI-подбор** — автоматическая генерация ключевых слов с помощью GPT
- 📁 **Проекты** — изоляция настроек для разных ниш
- 💳 **Подписки** — гибкая тарифная сетка
- ⚡ **Мгновенные уведомления** — получайте лиды в режиме реального времени

## 🏗️ Архитектура

Проект состоит из двух основных компонентов:

### 1. Control Bot (Aiogram)
- Интерфейс взаимодействия с пользователем
- Управление проектами, ключевыми словами и чатами
- Обработка платежей
- Интеграция с AI для подбора ключевых слов

### 2. Userbot Workers (Telethon)
- Мониторинг сообщений в Telegram-чатах
- Автоматическое вступление в группы
- Фильтрация сообщений по ключевым словам
- Отправка уведомлений пользователям

## 🛠️ Технологический стек

- **Python 3.10+**
- **aiogram 3.x** — Bot API
- **Telethon** — Userbot для мониторинга
- **PostgreSQL** — основная БД
- **SQLAlchemy** — ORM
- **Redis** — кэш и очереди
- **OpenAI API** — AI-функции

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/getlead.git
cd getlead
```

### 2. Создание виртуального окружения

```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните параметры:

```bash
cp .env.example .env
```

Обязательные параметры:
- `BOT_TOKEN` — токен бота от @BotFather
- `DATABASE_URL` — URL подключения к PostgreSQL
- `USERBOT_X_API_ID` — API ID от my.telegram.org
- `USERBOT_X_API_HASH` — API Hash от my.telegram.org
- `USERBOT_X_PHONE` — номер телефона для юзербота

### 5. Установка PostgreSQL

**Windows:**
1. Скачайте PostgreSQL с [официального сайта](https://www.postgresql.org/download/windows/)
2. Установите и создайте базу данных:

```sql
CREATE DATABASE getlead_db;
```

**Linux:**
```bash
sudo apt update
sudo apt install postgresql postgresql-contrib
sudo -u postgres createdb getlead_db
```

### 6. Установка Redis

**Windows:**
1. Скачайте Redis с [GitHub](https://github.com/microsoftarchive/redis/releases)
2. Запустите redis-server.exe

**Linux:**
```bash
sudo apt install redis-server
sudo systemctl start redis
```

### 7. Инициализация базы данных

База данных создается автоматически при первом запуске бота.

## 🚀 Запуск

### Запуск Control Bot

```bash
python main.py
```

### Запуск Userbot Workers

В отдельном терминале:

```bash
python run_userbot.py
```

При первом запуске юзербота вам нужно будет ввести код подтверждения из Telegram.

## 📖 Использование

### 1. Создание проекта

1. Запустите бота `/start`
2. Выберите "📁 Проекты"
3. Создайте новый проект (например, "Веб-разработка")

### 2. Добавление ключевых слов

1. Выберите "🔑 Ключевые слова"
2. Добавьте слова вручную или используйте AI-подбор
3. Пример: "сайт", "лендинг", "дизайн"

### 3. Добавление чатов

1. Выберите "💬 Чаты"
2. Добавьте ссылки на группы: `https://t.me/example_chat`
3. Бот автоматически вступит в чаты

### 4. Получение уведомлений

Когда в чате появится сообщение с вашими ключевыми словами, вы получите уведомление:

```
🔔 Найдено совпадение!

💬 Чат: Фриланс РФ
🔑 Ключевые слова: сайт, дизайн

📝 Текст сообщения:
Ищу дизайнера для создания сайта...

🔗 Перейти к сообщению
```

## 💳 Тарифы

- **Фрилансер** — 500₽/мес (5 чатов)
- **Стандарт** — 1500₽/мес (20 чатов)
- **Стартап** — 1000₽/мес (10 чатов)
- **Компания** — 3000₽/мес (50 чатов)

## 🗂️ Структура проекта

```
GetLead/
├── bot/                      # Control Bot (Aiogram)
│   ├── handlers/            # Обработчики команд
│   │   ├── common.py       # Основные команды
│   │   ├── projects.py     # Управление проектами
│   │   ├── keywords.py     # Ключевые слова
│   │   ├── chats.py        # Чаты
│   │   └── payment.py      # Платежи
│   ├── keyboards.py        # Клавиатуры
│   ├── middlewares.py      # Middleware
│   ├── states.py           # FSM состояния
│   └── texts.py            # Тексты сообщений
├── userbot/                 # Userbot Workers
│   ├── worker.py           # Воркер для мониторинга
│   └── matching.py         # Движок матчинга
├── database/                # База данных
│   ├── models.py           # Модели SQLAlchemy
│   ├── crud.py             # CRUD операции
│   └── database.py         # Настройка БД
├── config.py               # Конфигурация
├── main.py                 # Запуск Control Bot
├── run_userbot.py          # Запуск Userbot
└── requirements.txt        # Зависимости
```

## 🔧 Разработка

### Добавление новых обработчиков

1. Создайте файл в `bot/handlers/`
2. Зарегистрируйте роутер в `bot/handlers/__init__.py`
3. Добавьте тексты в `bot/texts.py`
4. Создайте клавиатуры в `bot/keyboards.py`

### Работа с базой данных

```python
from database import async_session_maker, UserCRUD

async with async_session_maker() as session:
    user = await UserCRUD.get_or_create(session, telegram_id=123456)
```

### Тестирование матчинга

```python
from userbot.matching import MatchingEngine

result = MatchingEngine.process_message(
    text="Ищу дизайнера для сайта",
    include_keywords=[...],
    exclude_keywords=[...]
)
```

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature-ветку (`git checkout -b feature/amazing-feature`)
3. Commit изменений (`git commit -m 'Add amazing feature'`)
4. Push в ветку (`git push origin feature/amazing-feature`)
5. Создайте Pull Request

## 📝 TODO

- [ ] Интеграция OpenAI для AI-подбора
- [ ] Интеграция ЮKassa для оплаты
- [ ] Интеграция CryptoBot для крипто-платежей
- [ ] Настройка Celery для очередей
- [ ] Логические фильтры (AND/OR)
- [ ] Статистика по лидам
- [ ] Экспорт лидов в CSV
- [ ] Webhook режим для бота
- [ ] Docker контейнеризация
- [ ] CI/CD pipeline

## 📄 Лицензия

MIT License

## 📧 Контакты

- Email: support@getlead.bot
- Telegram: @getlead_support

---

Сделано с ❤️ для фрилансеров и предпринимателей
